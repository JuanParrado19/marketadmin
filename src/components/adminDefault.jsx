import React from 'react';

const AdminDefault = () => {
    return (
        <div className="container mx-auto">
            <h1 className="text-2xl font-bold mb-4">Bienvenido al Administrador</h1>
            <p>Este es el administrador de su MarketPlace, al lado izquierdo encontrara la side bar en donde podra interactuar con todo el CRUD de su tienda</p>
            {/* Resto del código... */}
        </div>
    );
};

export default AdminDefault;
